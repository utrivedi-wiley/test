version: 0.2
phases:
 install:
   commands:
     - echo "install step"
 pre_build:
   commands:
     - echo "pre_build step"
 build:
   commands:
     - zip -r9 function.zip .
     - aws s3 sync --delete function.zip "s3://ujwaltest-codedeploy"
 post_build:
   commands:
     - echo "post_build step"
